server {
    listen 80;
    server_name _;
    
    # Enable debug logging
    error_log /dev/stdout debug;
    access_log /dev/stdout;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ =404;  # Change to return 404 instead of redirecting
    }
}
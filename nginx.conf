server {
    listen 80;
    server_name localhost;  # Changed to just localhost

    location / {
        root /usr/share/nginx/html;
        index index.html;
        autoindex on;  # Enable directory listing temporarily for debugging
    }
}